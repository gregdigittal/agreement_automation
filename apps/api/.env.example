# CCRS API — Phase 1a
# Copy to .env and set values. Never commit .env.

# Supabase (required)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# JWT validation (use same secret as Next.js app for next-auth session token)
JWT_SECRET=your-shared-secret-with-nextjs

# Optional: Azure AD (for production Entra ID tokens)
# AZURE_AD_CLIENT_ID=
# AZURE_AD_CLIENT_SECRET=
# AZURE_AD_TENANT_ID=common
# AZURE_AD_ISSUER=https://login.microsoftonline.com/{tenant}/v2.0

# Server
PORT=4000
CORS_ORIGIN=http://localhost:3000
